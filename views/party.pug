include ../partials/header.pug
h1 Qui prend quoi ?
h2=party.name
h3 Créé par #{party.author}

label(for="url") Partagez l'événement
input(id="url" value=`${url}`)

include ../partials/addComment.pug

h2 Les com's

each comment in party.items
  include ../partials/comment.pug
